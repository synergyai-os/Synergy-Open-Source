# Root Directory Cleanup - Quick Reference

**For**: Senior Architect Review  
**Date**: 2025-12-16

---

## TL;DR

Your root directory has **9 log files** (safe to delete) and **1 orphaned test directory** (decision needed). Everything else is either legitimate config or already gitignored.

---

## Safe to Delete Immediately ✅

```bash
rm -f audit-report.json
rm -f build-storybook.log
rm -f ci-output.log
rm -f debug-storybook.log
rm -f e2e-simplified-run.log
rm -f rate-limit-debug.log
rm -f test-detailed.log
rm -f test-output-syos-197.log
rm -f test-output.log
```

**Impact**: None (temporary debugging files)

---

## Needs Team Decision ⚠️

### 1. testsprite_tests/ Directory

**What**: Python-based test suite from TestSprite AI (testsprite.com)  
**Created**: November 13, 2025  
**Size**: 29 files (20 test cases, reports, configs)

**Options**:

- **Keep**: Move to `tests/testsprite/` for organization
- **Delete**: Archive reports to docs, remove directory

**Question**: Was this a one-time pilot or ongoing testing tool?

---

### 2. tokens.json File

**What**: 2,728-line DTCG token specification  
**Question**: Is this generated by `npm run tokens:build`?

**If Generated**: Add to `.gitignore`  
**If Source**: Keep and document in design system docs

---

## Add to .gitignore 🔧

```gitignore
# Build output (currently committed!)
www/

# Prevent future log clutter
*.log
*-report.json
audit-report.json

# TestSprite (if keeping)
testsprite_tests/tmp/
testsprite_tests/**/*.pyc
```

---

## Keep But Consider Moving 📁

### design-system-checklist.json

**Current**: Root directory  
**Suggested**: `dev-docs/master-docs/design-system-checklist.json`  
**Reason**: Better discoverability, consistent with other architecture docs

---

## Already Correct ✅

These are gitignored or legitimate configs:

- ✅ `storybook-static/` - Gitignored (line 41)
- ✅ `playwright-report/` - Gitignored (line 37)
- ✅ `test-results/` - Gitignored (line 1, 36)
- ✅ `__design-system-snapshots__/` - Partially gitignored (diffs only)
- ✅ All config files (`vite.config.ts`, `svelte.config.js`, etc.) - Expected

---

## Impact Assessment

| Action                            | Risk   | Impact                     |
| --------------------------------- | ------ | -------------------------- |
| Delete logs                       | None   | Cleaner root               |
| Gitignore www/                    | None   | Prevent accidental commits |
| Move design-system-checklist.json | Low    | Better organization        |
| Delete testsprite_tests/          | Medium | Depends on usage           |

---

## Detailed Report

See `ROOT-FILES-ANALYSIS.md` for full analysis including:

- Purpose of each file/directory
- Build process documentation
- .gitignore recommendations
- File size/content details

---

## Quick Win Command

```bash
# Clean up logs + update .gitignore in one go
rm -f *.log *-report.json && \
echo -e "\n# Build outputs\nwww/\n\n# Log files\n*.log\n*-report.json" >> .gitignore
```

**Time to Execute**: 30 seconds  
**Files Cleaned**: 9  
**Risk**: None
