# ============================================
# SynergyOS Environment Variables
# ============================================
# This is a PUBLIC template - safe to commit to git
# DO NOT put real secrets here - use .env with 1Password references
#
# For local dev with secrets: op run -- npm run dev
# See: dev-docs/2-areas/secrets-management.md
# ============================================

# ============================================
# Convex (Database & Auth)
# ============================================
# Production deployment URL (CLIENT-SIDE - must start with PUBLIC_)
PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud

# Your production domain for auth redirects (SERVER-SIDE)
# Should be your actual domain (e.g., https://www.synergyos.ai)
CONVEX_SITE_URL=https://your-domain.com

# Deploy key for CI/CD builds (SERVER-SIDE)
# Get from: https://dashboard.convex.dev → Settings → Deploy Key
CONVEX_DEPLOY_KEY=prod:your-deploy-key-here

# Optional: Deployment name for reference
CONVEX_DEPLOYMENT=your-deployment-name

# ============================================
# PostHog (Analytics)
# ============================================
# PostHog project key (CLIENT-SIDE - must start with PUBLIC_)
# Get from: https://posthog.com → Project Settings → API Key
PUBLIC_POSTHOG_KEY=phc_your_key_here

# PostHog host URL (CLIENT-SIDE - must start with PUBLIC_)
PUBLIC_POSTHOG_HOST=https://us.i.posthog.com

# ============================================
# Environment-Specific Notes
# ============================================
# DEV: Use 1Password CLI (op run -- npm run dev)
# PROD: Set these in Vercel Environment Variables
# See: dev-docs/2-areas/secrets-management.md
