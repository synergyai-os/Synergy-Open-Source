/**
 * Authority has no Convex queries; calculations are pure functions.
 * SYOS-707 scaffold file to satisfy domain structure.
 */

export function describeAuthorityModel(): string {
	return 'Authority is derived from roles and circle types; no persisted queries.';
}
