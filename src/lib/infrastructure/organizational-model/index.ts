/**
 * Organizational Model Infrastructure - Public API
 *
 * Re-exports from api.ts to enable clean imports:
 * import { useCircles } from '$lib/infrastructure/organizational-model'
 */

export * from './api';
