# Convex Features Layer

Feature modules live here and compose core domains without introducing cross-feature dependencies. Add new feature directories under this folder with `queries.ts`, `mutations.ts`, `rules.ts`, `index.ts`, and co-located tests when implementing functionality.
