# AI Tools Components (Temporary)

**TODO**: AI Tools will become its own module (future work)

See: SYOS-425 Component Reorganization

---

## Current Location

This folder is temporarily in `src/lib/components/ai-tools/` but will eventually be moved to its own module structure once the AI Tools module is created.

## Components

- `MetricsForecast.svelte` - Metrics forecasting visualization
- `ToolComparisonTable.svelte` - Tool comparison table
